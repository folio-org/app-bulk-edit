{
    "name": "${project.name}",
    "version": "${project.version}",
    "description": "${project.description}",
    "platform": "complete",
    "dependencies": [
      {
        "name": "app-fqm",
        "version": "^2.1.0-SNAPSHOT",
        "preRelease": "only"
      },
      {
        "name": "app-search",
        "version": "^1.0.0-SNAPSHOT",
        "preRelease": "only"
      },
      {
        "name": "app-marc-storage",
        "version": "^1.0.0-SNAPSHOT",
        "preRelease": "only"
      },
      {
        "name": "app-inventory",
        "version": "^1.0.0-SNAPSHOT",
        "preRelease": "only"
      },
      {
        "name": "app-data-import",
        "version": "^1.0.0-SNAPSHOT",
        "preRelease": "only"
      }
    ],
    "modules": [
      {
        "name": "mod-bulk-operations",
        "version": "2.4.0-SNAPSHOT.2664",
        "preRelease": "only"
      }
    ],
    "uiModules": [
      {
        "name": "folio_bulk-edit",
        "version": "latest",
        "preRelease": "only"
      }
    ]
  }
